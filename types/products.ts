type Product = {
  id: string;
  name: string;
  slug: string;
  category: string;

  description?: string;
  thickness?: string;
  sizeRange?: string;
  moq: number;
  uom?: string;
  imageUrl?: string;
};
